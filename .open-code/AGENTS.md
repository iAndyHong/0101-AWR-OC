<!-- 這是給 AI 代理人看的私有註解：
     請注意，如果遇到連線不穩定請主動提醒 Andy 
     1. 載入順序：先載入「個人層級」，再載入「專案層級」
        載入時會先讀取 ~/.config/opencode/AGENTS.md 來建立基礎的身分與行為基準（例如你的稱呼、通用開發語言禁令等）。
    2. 覆蓋與執行優先權：專案層級 (.open-code/AGENTS.md) 優先權最高
        在實際執行任務時，「專案層級」的規範會覆蓋「個人層級」中衝突的部分。
     -->

### 0. 核心指令解析原則
- **語音輸入修正**：解析用戶指令時，必須優先參考 `.open-code/rules/stt-correction.md` 對發音模糊或語音輸入導致的誤植詞彙進行語意修正。

### 1. 標準工作流程
1. **任務啟動**：必須執行 `say -r 220` 提醒 Andy。語音內容可根據任務性質具備彈性（例如："開始修正邏輯"、"開始編譯驗證"）。
2. **實作與驗證**：遵循零警告政策執行開發與編譯。
3. **文檔同步 (CRITICAL)**：凡是 `HA/` 資料夾下有任何版本推進（版本號變更）或功能更新，必須同步更新 `HA/Logic.md` 中的演進紀錄。
4. **任務完成**：必須執行 `say -r 220` 報告狀態。語音內容應清楚說明結果（例如："修正完成，編譯成功"、"邏輯已更新，待命中"）。